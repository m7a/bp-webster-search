`mdvl-websters-dictionary` package for the scripts proposed in
https://github.com/dnmfarrell/WebsterSearch.
Builds a static index and provides command `webster_dict` to search the
dictionary. Build the package by issuing `ant package`.
